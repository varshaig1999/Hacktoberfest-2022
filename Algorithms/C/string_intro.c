#include <stdio.h>

int main()
{
    // char str[] = {'P','h','a','h','a','m','\0'}; '\0' is a null character that shows string is terminated.
    char str[] = "Phaham"; //← This is shortcut way of represnting strings, here null character automatically included by C.
    return 0;
}