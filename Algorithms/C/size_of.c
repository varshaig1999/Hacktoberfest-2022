#include <stdio.h>

int main()
{
    char a;           //% will give remainder
    int b;            // == is for comparison anf = is for assigning values.
    float c;
    double d;
    printf("size of char is %ld\n", sizeof(a));
    printf("size of int is %ld\n", sizeof(b));
    printf("size of float is %ld\n", sizeof(c));
    printf("size of double is %ld\n", sizeof(d));

    return 0;
}